import matplotlib.pyplot as plt

def outlier_histogram(data, x, y, limit=0.8):

   out_names = outlier_names[ outlier_names.prct < 0.8].names.tolist()
   fig = plt.figure()

   for xx in xrange(len(out_names)):
     ax = fig.add_subplot(2,2,xx+1)
     plt.hist( df_cln.ix[:, out_names[xx]], bins=100, label = out_names[xx])
     ax.set_title( out_names[xx] )
     #plt.xlabel( out_names[xx])

plt.show()
